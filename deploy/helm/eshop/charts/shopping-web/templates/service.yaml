{{- $name := "shopping-web" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  labels:
    app: shopping-web
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: shopping-web
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
