{{- $name := "catalog-api" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  labels:
    app: catalog-api
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: catalog-api
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
